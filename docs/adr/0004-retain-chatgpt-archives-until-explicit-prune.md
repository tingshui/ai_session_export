# Retain ChatGPT archives until explicit prune

An archived ChatGPT conversation remains on disk when it disappears from live discovery or moves outside the approved Project allowlist; absence only stops future updates. Deletion requires an explicit, human-confirmed prune backed by an authoritative official export, and derived memory is never deleted as a side effect of archive pruning, because discovery-window gaps and Project moves are not safe deletion signals.
